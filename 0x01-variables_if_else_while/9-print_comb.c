#include <stdio.h>
/**
 * main - all combination of single digits
 * Return: Always 0
 */
int main(void)
{
	int x;
	int y = , ;
		for (x = 0; x < 10; x++)
			putchar(x);
			putchar(y);
		putchar('\n');
		return (0);
}
