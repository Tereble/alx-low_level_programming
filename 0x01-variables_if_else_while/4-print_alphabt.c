#include <stdio.h>
#include <ctype.h>
/**
 *  main - Entry
 *  Retun: Always 0
 */
int main(void)
{
	int x;
		for (x = "a", x <= "z", x++)
		{
			if (x != "e" && != "q")
			{
				putchar(x);
			}
		}
		putchar('\n);
		return (0);
}
