#include <stdio.h>
#include <ctype.h>
/**
 * main - lower and uppercase
 * Return: Always 0
 */
int main(void)
{
	int x;
		for (x = 'A'; x <= 'Z'; x++)
			char small;
				putchar(small);
		for (x = 'A'; x <= 'Z'; x++)
			char big;
				putchar(big);
		putchar('\n');
	return (0);
}
