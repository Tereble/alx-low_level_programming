#include <stdio.h>
#include <ctype.h>
/**
 * main - lower and uppercase
 * Return: Always 0
 */
int main(void)
{
	int x;
		for (x = 'A'; x <= 'Z'; x++)
			putchar(small);
		for (x = 'A'; x <= 'Z'; x++)
			putchar(x);
		putchar('\n');
	return (0);
}
